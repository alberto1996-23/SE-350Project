// This should hold your backend URL.

// Example idea:

// base URL for backend API

// So React does not hardcode "http://localhost:8000" everywhere.
export const API_BASE_URL = 'http://127.0.0.1:8000/api'